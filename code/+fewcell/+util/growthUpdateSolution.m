function uu0= growthUpdateSolution(uu,growth,bactNodes)
% Each time the integration is interrupted, this function updates the system state. It dilutes all the existing
% bacteria, kills inner bacteria if necessary and grows new bacteria outwards
  nBact= size(bactNodes,2);
  
  % TODO: growth.stepBact   (bacterial state idx for each step
end
